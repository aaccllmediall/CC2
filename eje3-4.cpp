#include <iostream>
#include <cstdlib>  // constains some math functions

using namespace std;

int difference(const int x, const int y){
    int diff=abs(x-y);  // abs(n) returns absolute value of n
    //return diff;
}
int main(){
    cout << difference(24, 1238);       // no retorna nada, asi que vota
    return 0;                               // un valor basura
}

